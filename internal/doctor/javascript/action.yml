name: 'Doctor JavaScript'
description: 'Perfil JS/TS do Doctor (invocado via registry central).'

runs:
  using: composite
  steps:
    - name: Info
      shell: bash
      run: |
        set -euo pipefail
        echo "Use internal/doctor (registry). Perfil javascript-source|typescript-source."
