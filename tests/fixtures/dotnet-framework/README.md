# Placeholder fixture for classic ASP.NET / .NET Framework (Windows runner).
# MVP detects via TargetFrameworkVersion / web project markers.
# Full compile coverage runs on windows-latest when a sample is expanded.
